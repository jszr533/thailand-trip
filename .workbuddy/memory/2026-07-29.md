# 2026-07-29

**酒店凭证接入（进行中）**：用户要把两个酒店「入住凭证/确认函」接进「必带物品 → 🏨 酒店预订单」，要求点开能分别打开两个。两家酒店：①亚洲酒店 Asia Hotel Bangkok（确认号 321-10916245，bkk_hotel/d2_1/d3_1）；②塔佩幸福旅舍 Thapae Happy House Chiang Mai（确认号 1128149412246066，cnx_hotel/d3_4/d4_6/d5_3）。当前对话与本地工作区均无这两个凭证文件（搜 conversation_search 0 结果、grep 无 base64/PDF），判定为早期会话附件未带过来。已用 AskUserQuestion 确认走「用户重新发两个文件」方案（推荐项被选中）。待用户重新发送两个凭证（PDF/图片均可，需分辨对应哪家酒店），计划：内嵌为 base64 data URI 进 index.html（离线可开），把 line 673 的静态 chip 改成两个按钮「🏨亚洲酒店凭证」「🏨塔佩幸福旅舍凭证」，各自新标签打开对应 voucher；同时 public/index.html 同步、node 校验、双源推送。

**本回合（3 页串联导航重构）**：用户要首页=世界地图足迹(上下加文字/图画/背景装饰)，点开地图→城市→行程点，3个网页串起来；历史行程由我定位置。用 AskUserQuestion 确认：编辑入口放在城市页的「行程模块」按钮，打开原做攻略网页(功能不变仅换入口)。实现：① 去掉 `view-home/view-trip` 二元切换，改 `currentFlow`(home/cities/points/trip) + `setFlow()` + `setNavActive()`，body 加 `flow-*` 类；顶部 `#flowNav` 4 步面包屑(首页›城市›行程点›我的行程)。② `#homeView` 拆为 `#pageHome`(hero装饰带[标题+副文案+统计pill]+世界地图+foot装饰带[统计+浏览城市/我的行程 CTA]) / `#pageCities`(大洲钻取+去过+想去+城市页「🗂️行程模块」卡片按钮 setFlow('trip')) / `#pagePoints`(按城市聚合所有行程的 spots，按 行程→天→时间 分组渲染，附 POI_DB 推荐库可「➕加入」)。③ JS 新增 `renderHomeHero/renderCities/openFlowCity/openCity/renderPoints`；地图点/visited卡/drill城市chip→openFlowCity/openCity；`render()` 按 currentFlow 分发。④ 首页 hero 内联 SVG 虚线弧+飞机+发光点装饰；行程点用 buildSpotLinks 复用编辑器同款链接。已上线 5f3a5de。
**经验**：单文件 SPA 做「多页串联」最稳的是「状态机 + body class 控制显隐 + 面包屑导航」三件套，比真多文件简单且零路由依赖；装饰带用渐变 panel + 内联 SVG 比外部图省事。


**本回合（行程点库自动同步 + 行程按实际时间标注）**：用户要行程中的行程点自动同步进行程点库(不论已结束/待开始)，且行程按实际时间标注是否结束/开始。实现：新增 spotCitiesOf(day,spot)(addr/name命中已知城市→loc前缀bkk_/cnx_→回退day.city slug拆多城市) + collectTripSpots(onlyTrip)(汇总所有TRIPS的spots→{城市:[{trip,day,spot}]}) + gotoSpot(tripId,dayId)(跳到对应行程/天/编辑视图)；重写 poiCities()(=POI_DB城市 ∪ 有行程点城市) 与 renderPoi()(每城市分「我的行程点」按trip分组带状态pill + 「推荐地点」可加入)；行程点行可点跳转。新增 parseMD/todayMD/cmpMD/tripTimeStatus + 全局 ST_LABEL(upcoming待开始/active进行中/closed已结束)；tripTimeStatus 按 start/end(M.D格式、当前年比较、支持跨年)自动算，手动 status=closed 视为已结束(可提前归档/取消)；renderTripBar 与 renderPoints(第3页)徽标改用 tripTimeStatus。CSS 加 .tc-badge.upcoming 及 .poi-row.mine/.poi-tag/.poi-trip-sub/.st-pill。已上线 025e274。
局限：日期仅存 M.D 无年份，跨年/去年行程的待开始/已结束按当前年推测（去年12月行程会误显待开始）；需精确年份时再给行程加年份字段。


**本回合（行程点库→旅游日志）**：用户要①行程里勾选过的点、在行程归档结束后、库里显示「去过几次」；②每个行程点可多次评价，评价显示日期+星级/评分。实现：① 新增 SPOT_REVIEWS(字典，key=城市:::名称→[{date,rating,text}])，loadReviews/persistReviews(localStorage spot_reviews)；computeVisitMap() 仅统计 tripTimeStatus==closed 的行程中 checked(实际去过) 的 spot，按 城市+名称 计数；② renderPoi 每个 poi-row(我的行程点+推荐地点) 加 reviewBlock()：去过N次标 + 「✍️评价」按钮 + 「⭐均分·N条」可展开评价列表(每条 日期+星级+文字+删除)；新增 #reviewModal(5星点选+date+textarea)，openReviewModal/setReviewStar/renderReviewStars/saveReview/deleteReview/toggleReviews；评价按日期倒序，多次独立保留；key 用 hashId 生成安全 DOM id。已上线 cd1f4db。
**体验细节**：我的行程点行(可跳编辑)内点评价按钮用 event.stopPropagation 防误触跳转；推荐地点行也允许评价(同名点会合并展示，如行程与POI_DB都叫双龙寺)；去过点数=已勾选且行程已结束,未勾选或行程未结束不计数。

**本回合（去掉扁平「城市景点库」，改为「城市→该城市行程点库」逐级钻取）**：用户不满有一个独立平铺的「城市景点库」弹窗(poiModal)，要求逻辑关系为 我的足迹→点开→我去过哪些城市(城市是个库)→点开某城市→它其中行程点的库。重构：① 删除 poiModal 弹窗及其两处入口按钮(首页地图头/行程条「📚行程点库」)，删除对应 JS(openPoi/poiCities/renderPoi/setPoiOnlyTrip/gotoSpot/var poiOnlyTrip)，用行删除脚本精准移除(删函数后残留一个多余 `}`，已修)；② 把「去过几次+多次评价」(reviewBlock) 从已删的 poiModal 搬进城市行程点库 renderPoints，每个行程点/推荐点都带 reviewBlock，新增 computeVisitMap 调用与 refreshReviewHost()(评价保存/删除后刷新正确容器：recModal优先、其次points页、否则render())；③ 城市页强化为「📚我去过的城市·我的城市库」，文案明确「点开城市→它的行程点库」；行程点页标题改为「X的行程点库」。评价保存/删除原依赖 poiModal 的 renderPoi，已全部改为 refreshReviewHost，无遗留未保护引用(poiDaySel/poiModal 仅剩 getElementById 空值保护)。node --check 通过。

**本回合（年份显示 + 城市库同点合并防占空间）**：用户反馈①手机上「去过几次」应显示年份；②同一点去多次、把每次的行程/名字/时间都列出来会占地方。实现：① 给行程加 `year` 字段——tripMetaModal 加「年份」数字输入(默认当年)、editTripMeta 回填、saveTripMetaFromForm 存、createTrip/种子行程/loadAllTrips 迁移都补默认当年；computeVisitMap() 改为返回 {count, years:[]}(仅 closed 行程、按 t.year 归因年份、去重排序)；reviewBlock() 参数由 visitCount 改为 visit 对象，「去过 N 次 · 2024 2025」年份标签(.poi-years)；世界地图足迹点/历史树行程标签也补 (year)。② 城市库 renderPoints 重写：同城同点按「名称」聚合为**一行卡片**(lib-spot)，多次出现时卡片下方加可折叠的「📋 行程记录」(默认 collapsed，点卡片 toggleSpotLog 展开)，每行=行程标题(年份)·天·时间，可点 jumpToSpot 跳去对应行程编辑(setActiveTrip+setFlow('trip'))；不再按 trip→day 平铺重复行，解决占空间。新增 CSS：.pt-rec-h/.lib-spot/.lib-main/.spot-log.collapsed/.log-row/.log-year/.log-edit + @media(max-width:560px) 收紧间距/字号。recs 推荐块 reviewBlock 传参同步改 {count:0,years:[]}。node --check 通过，已上线 925bc67。
经验：旅游日志类「多次访问同一地点」优先用「一行卡片 + 折叠明细」而非重复行，手机端尤其省空间；年份等时间维度一定要落到数据字段(不要靠当前年推测)，否则跨年展示会错乱。
