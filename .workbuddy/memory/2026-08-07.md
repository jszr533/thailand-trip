# 2026-08-07

**本回合（修复「我的世界足迹页」仍出现入境须知的泄漏路径）**：用户再次反馈足迹页出现入境须知。实测(jsdom)证明 pageHome(flow-home) HTML 本身无入境须知 DOM（仅标题+地图+搜索）；入境须知只在 ①城市指南(pagePoints, 经 城市页→点城市 触发) ②我的行程编辑器静态块(line 801) 两处。经排查定位到**真实泄漏点**：足迹页搜索框 `homeSearch`(line 1843) 对「去过」的城市结果 onclick 调 `openCity`（直达城市指南带入境须知），绕过了"城市页→点城市"规则；而地图点调 `openFlowCity`（正确）。修复：将搜索结果的 visited 分支从 `openCity` 改为 `openFlowCity`，与地图点一致——足迹页点城市一律先去城市页，再由城市页点城市才进指南。新增 qa_probe2.js 回归测试（含「足迹页搜索曼谷→点去过结果→应落 flow-cities 而非 flow-points」断言）验证通过。校验：validate 单脚本 263049 字符 ALL GOOD；qa_runtime 全过 0 错误（boot 的 tripCode.then 报错仅 jsdom stub 环境产物，真实浏览器无影响）；qa_probe2 确认搜索路径修复。双源同步、待推送。

注：用户此前多次提"首页/足迹页有入境须知"，真因即此搜索直达泄漏（非缓存），已根治。

**本回合（首尔行程"按之前的行程逻辑排版"）**：用户要求首尔行程按之前（泰国）行程逻辑排版。排查发现二者数据结构/字段已完全一致（spot 用 `warning` 标记、day 用 date/weekday/weather/city——与泰国 DAYS 对齐），唯一实质差异在**行程内地图**：`renderMap` 仅认 `bangkok`/`chiangmai` 两种底图（line 4078 `type` 判断 + 4068 跨城判断），首尔 day.city='首尔' 会被默认画成**曼谷底图**；且首尔所有 spot 的 `loc` 原为 `null`，导致地图无任何点位/路线/交通建议（泰国每个点都配了 LOCATIONS 坐标）。修复（commit 486c1ca）：① `drawBase` 增加 `seoul` 分支（汉江浅蓝带 + 仁川/首尔站/明洞/南山/市厅/清溪川/东大门/广藏/弘大/仁寺洞/景福宫/圣水洞/乐天/COEX/南怡岛 区域标签 + 道路线）；② `LOCATIONS` 注册 16 个首尔坐标 key（如 sel_myeongdong[2,4]、sel_icn[-44,-22]、sel_nami[46,-30] 等，汉江 y≈10 横贯，江北 y<10 江南 y>10）；③ `renderMap` 的 type 判断加 `day.city.includes('首尔') ? 'seoul' : 'bangkok'`；④ `SEOUL_DAYS` 全部首尔境内 spot 的 `loc:null` 改为对应 key（南京出发/抵达仍 null）。校验：根文件语法 OK；node 校验 13 个首尔 loc 引用全部命中 LOCATIONS、无拼写错；drawBase/renderMap seoul 分支均已注册。双源同步、待推送（后台重试，沙箱→GitHub 网络偶发重置）。

**部署确认（用户问"之前的网址能打开吗"）**：WebFetch 实测 GitHub Pages(https://jszr533.github.io/thailand-trip/) 与 Vercel(https://thailand-trip-gilt.vercel.app/) 均 HTTP 200、内容完整渲染，线上已含「首尔·圣诞周末」行程与行程点解耦文案。但 `git status` 显示 `origin/main [gone]`、本地最新 486c1ca/5f57473 此前多次推送因沙箱→GitHub 网络重置失败。本次重新 `git push origin main` 成功（486c1ca..5f57473），网络已恢复，首尔地图首尔底图排版修复已上线。两个站点会自动重新部署，用户强刷即可见最新版。

**本回合（新增首尔圣诞周末行程）**：用户在"我的行程"加首尔行程，南京出发，今年圣诞周五(12.25)→周日(12.27)，要圣诞氛围+美食，并"安排机票和行程"。关键技术点：行程数据存浏览器 `localStorage`(LS_TRIPS)，导入机制(`importJSON`/`importTripState`)只按当天 id 合并、无法新增独立行程。做法：① 在 `POI_DB` 新增 `'首尔'` 城市指南（圣诞+美食+景点，符合"行程点库只收城市通用推荐"要求）；② 新增固定 ID 种子行程 `SEOUL_DAYS`/`SEED_SEOUL_ID='seed_seoul_xmas_2026'`，3 天 20 点；③ `loadAllTrips` 末尾调 `ensureSeoulSeed()`——若 TRIPS 不含该 id 则 push 并 `persistAll()`，使**现有用户（已有泰国行程）刷新已部署页面即自动多出首尔独立行程**，不覆盖泰国；④ `deleteTrip` 删除该种子时写 `localStorage.seoul_seed_v1='1'`，防刷新重生。航班用真实班次：去程 MU579/KE5860 08:15→11:15、返程 MU580/KE5859 12:15→13:25（晚班备选 MU2922 20:15→21:45），flightNo 字段可点开 FlightRadar24。校验：根 index.html `new Function` 1 脚本 ALL GOOD；单独 eval `SEOUL_DAYS` 得 3 天 20 点、字段完整。root/public 双源同步、本地提交 2f18634，GitHub 推送起后台重试（沙箱网络重置）。
排期注意：12.25 确为 2026 周五、12.27 周日（已核对日历）。我已用 WebSearch 核实 NKG⇄ICN 真实航班时刻，但**未代订票**——仅计划/参考。
遗留：首尔酒店"待订"（用户未提供确认号，类比泰国行程的亚洲酒店/塔佩幸福旅舍）；如需可后续补。

**本回合（首页去除"详细行程"）**：用户再次强调"首页不需要任何详细行程"。实测确认 pageHome DOM 仅标题+地图+搜索+「已点亮 N 个城市」统计，无任何行程文字/列表。唯一带"行程"语义的是 `renderHomeMap` 中按 `orderedCities()`（行程天顺序）绘制的城市连线弧线——即首页地图上的"行程路线"。修复：移除首页地图的弧线绘制（`homeMapArcs` 置空），仅保留去过城市的足迹圆点（年份着色属"足迹"非"行程"）。首页现彻底无详细行程。validate 通过；root/public 双源同步、本地已提交（ahead 1），GitHub 推送因沙箱网络重置暂未成功、已起后台重试。

**本回合（行程点库与我的行程彻底解耦）**：用户明确"每次的行程攻略只要放在我的行程里面，别的首页、城市、行程点都不用"，并选定"彻底与行程解耦"。实测：逐日行程（航班+按天安排+时间点）本来就只在「我的行程」页；首页/城市/行程点页都不显示时间安排。唯一与行程挂钩的是「行程点」页的"城市行程点总库"——它从 `TRIPS` 拉取行程点并显示"出现在 N 个行程"，且 `computeVisitMap()` 也扫描 `TRIPS`（已结束行程里勾选的点）来算"去过几次+年份"。修复：① `renderPoints` 改为只从 `POI_DB[city]`（城市通用推荐地点）取点，删除 TRIPS 拉取块；② 删除"出现在 N 个行程"字样（`lib-trips`）及相关死 CSS；③ `computeVisitMap` 改为仅统计 `SPOT_REVIEWS`（评价系统）得出"去过几次+年份"，与行程彻底脱钩；④ 更新两处说明文案（去掉"我的行程点/已结束行程"字样）。校验注意：`validate.js` 实际读 `outputs/index.html`（旧副本，勿被其"121531 chars ALL GOOD"误导），对根 `index.html` 用 `new Function` 校验确认 1 脚本 ALL GOOD。root/public 双源同步、本地已提交 1ef5165，GitHub 推送起后台重试（沙箱网络重置）。
