# 2026-08-11

**本回合（按航班生成马来西亚人文行程并加入 App）**：用户提供航班截图（10.2 南京→厦门→槟城、10.4 槟城→古晋、10.6 古晋→吉隆坡、10.8 吉隆坡→南京），要求按之前行程逻辑生成偏人文的旅行攻略计划。实施：
- 新增 `MALAYSIA_DAYS`（7 天 6 晚，2026.10.2-10.8），航班、人文景点、美食按实际时间排版；主题聚焦槟城华人南洋/娘惹史、古晋砂拉越原住民与河城文化、吉隆坡独立记忆与马华印多元宗教。
- 新增 `SEED_MALAYSIA_ID` / `buildMalaysiaTrip` / `ensureMalaysiaSeed`，在 `loadAllTrips` 加载后自动补齐；`deleteTrip` 记录用户手动删除，避免刷新后重复出现。
- `POI_DB` 补全「槟城」「古晋」「吉隆坡」三城通用推荐地点，均带真实 addr。
- `LOCATIONS` 注册槟城/古晋/吉隆坡共 32 个坐标 key；`drawBase` 新增 `penang`/`kuching`/`kl` 三城底图；`renderMap` type 判断支持这三城，保持与泰国/首尔行程一致的地图排版。
- 新增 `马来西亚人文线行程方案.md` 攻略文档；新增 `qa_malaysia.js` 静态数据校验脚本。
- 校验：根 `index.html` 语法 OK；`qa_runtime.js`、`qa_deep.js` 回归全过；`qa_malaysia.js` 16 项校验全过。双源同步并推送成功（commit b467479）。
