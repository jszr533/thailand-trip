services:
  - type: web
    name: thailand-trip-sync
    env: node
    startCommand: node server/server.js
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "10000"
      - key: NODE_VERSION
        value: "22"
