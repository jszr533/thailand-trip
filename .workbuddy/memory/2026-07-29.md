# 2026-07-29

**酒店凭证接入（进行中）**：用户要把两个酒店「入住凭证/确认函」接进「必带物品 → 🏨 酒店预订单」，要求点开能分别打开两个。两家酒店：①亚洲酒店 Asia Hotel Bangkok（确认号 321-10916245，bkk_hotel/d2_1/d3_1）；②塔佩幸福旅舍 Thapae Happy House Chiang Mai（确认号 1128149412246066，cnx_hotel/d3_4/d4_6/d5_3）。当前对话与本地工作区均无这两个凭证文件（搜 conversation_search 0 结果、grep 无 base64/PDF），判定为早期会话附件未带过来。已用 AskUserQuestion 确认走「用户重新发两个文件」方案（推荐项被选中）。待用户重新发送两个凭证（PDF/图片均可，需分辨对应哪家酒店），计划：内嵌为 base64 data URI 进 index.html（离线可开），把 line 673 的静态 chip 改成两个按钮「🏨亚洲酒店凭证」「🏨塔佩幸福旅舍凭证」，各自新标签打开对应 voucher；同时 public/index.html 同步、node 校验、双源推送。

**本回合（3 页串联导航重构）**：用户要首页=世界地图足迹(上下加文字/图画/背景装饰)，点开地图→城市→行程点，3个网页串起来；历史行程由我定位置。用 AskUserQuestion 确认：编辑入口放在城市页的「行程模块」按钮，打开原做攻略网页(功能不变仅换入口)。实现：① 去掉 `view-home/view-trip` 二元切换，改 `currentFlow`(home/cities/points/trip) + `setFlow()` + `setNavActive()`，body 加 `flow-*` 类；顶部 `#flowNav` 4 步面包屑(首页›城市›行程点›我的行程)。② `#homeView` 拆为 `#pageHome`(hero装饰带[标题+副文案+统计pill]+世界地图+foot装饰带[统计+浏览城市/我的行程 CTA]) / `#pageCities`(大洲钻取+去过+想去+城市页「🗂️行程模块」卡片按钮 setFlow('trip')) / `#pagePoints`(按城市聚合所有行程的 spots，按 行程→天→时间 分组渲染，附 POI_DB 推荐库可「➕加入」)。③ JS 新增 `renderHomeHero/renderCities/openFlowCity/openCity/renderPoints`；地图点/visited卡/drill城市chip→openFlowCity/openCity；`render()` 按 currentFlow 分发。④ 首页 hero 内联 SVG 虚线弧+飞机+发光点装饰；行程点用 buildSpotLinks 复用编辑器同款链接。已上线 5f3a5de。
**经验**：单文件 SPA 做「多页串联」最稳的是「状态机 + body class 控制显隐 + 面包屑导航」三件套，比真多文件简单且零路由依赖；装饰带用渐变 panel + 内联 SVG 比外部图省事。


**本回合（行程点库自动同步 + 行程按实际时间标注）**：用户要行程中的行程点自动同步进行程点库(不论已结束/待开始)，且行程按实际时间标注是否结束/开始。实现：新增 spotCitiesOf(day,spot)(addr/name命中已知城市→loc前缀bkk_/cnx_→回退day.city slug拆多城市) + collectTripSpots(onlyTrip)(汇总所有TRIPS的spots→{城市:[{trip,day,spot}]}) + gotoSpot(tripId,dayId)(跳到对应行程/天/编辑视图)；重写 poiCities()(=POI_DB城市 ∪ 有行程点城市) 与 renderPoi()(每城市分「我的行程点」按trip分组带状态pill + 「推荐地点」可加入)；行程点行可点跳转。新增 parseMD/todayMD/cmpMD/tripTimeStatus + 全局 ST_LABEL(upcoming待开始/active进行中/closed已结束)；tripTimeStatus 按 start/end(M.D格式、当前年比较、支持跨年)自动算，手动 status=closed 视为已结束(可提前归档/取消)；renderTripBar 与 renderPoints(第3页)徽标改用 tripTimeStatus。CSS 加 .tc-badge.upcoming 及 .poi-row.mine/.poi-tag/.poi-trip-sub/.st-pill。已上线 025e274。
局限：日期仅存 M.D 无年份，跨年/去年行程的待开始/已结束按当前年推测（去年12月行程会误显待开始）；需精确年份时再给行程加年份字段。


**本回合（行程点库→旅游日志）**：用户要①行程里勾选过的点、在行程归档结束后、库里显示「去过几次」；②每个行程点可多次评价，评价显示日期+星级/评分。实现：① 新增 SPOT_REVIEWS(字典，key=城市:::名称→[{date,rating,text}])，loadReviews/persistReviews(localStorage spot_reviews)；computeVisitMap() 仅统计 tripTimeStatus==closed 的行程中 checked(实际去过) 的 spot，按 城市+名称 计数；② renderPoi 每个 poi-row(我的行程点+推荐地点) 加 reviewBlock()：去过N次标 + 「✍️评价」按钮 + 「⭐均分·N条」可展开评价列表(每条 日期+星级+文字+删除)；新增 #reviewModal(5星点选+date+textarea)，openReviewModal/setReviewStar/renderReviewStars/saveReview/deleteReview/toggleReviews；评价按日期倒序，多次独立保留；key 用 hashId 生成安全 DOM id。已上线 cd1f4db。
**体验细节**：我的行程点行(可跳编辑)内点评价按钮用 event.stopPropagation 防误触跳转；推荐地点行也允许评价(同名点会合并展示，如行程与POI_DB都叫双龙寺)；去过点数=已勾选且行程已结束,未勾选或行程未结束不计数。

**本回合（去掉扁平「城市景点库」，改为「城市→该城市行程点库」逐级钻取）**：用户不满有一个独立平铺的「城市景点库」弹窗(poiModal)，要求逻辑关系为 我的足迹→点开→我去过哪些城市(城市是个库)→点开某城市→它其中行程点的库。重构：① 删除 poiModal 弹窗及其两处入口按钮(首页地图头/行程条「📚行程点库」)，删除对应 JS(openPoi/poiCities/renderPoi/setPoiOnlyTrip/gotoSpot/var poiOnlyTrip)，用行删除脚本精准移除(删函数后残留一个多余 `}`，已修)；② 把「去过几次+多次评价」(reviewBlock) 从已删的 poiModal 搬进城市行程点库 renderPoints，每个行程点/推荐点都带 reviewBlock，新增 computeVisitMap 调用与 refreshReviewHost()(评价保存/删除后刷新正确容器：recModal优先、其次points页、否则render())；③ 城市页强化为「📚我去过的城市·我的城市库」，文案明确「点开城市→它的行程点库」；行程点页标题改为「X的行程点库」。评价保存/删除原依赖 poiModal 的 renderPoi，已全部改为 refreshReviewHost，无遗留未保护引用(poiDaySel/poiModal 仅剩 getElementById 空值保护)。node --check 通过。

**本回合（年份显示 + 城市库同点合并防占空间）**：用户反馈①手机上「去过几次」应显示年份；②同一点去多次、把每次的行程/名字/时间都列出来会占地方。实现：① 给行程加 `year` 字段——tripMetaModal 加「年份」数字输入(默认当年)、editTripMeta 回填、saveTripMetaFromForm 存、createTrip/种子行程/loadAllTrips 迁移都补默认当年；computeVisitMap() 改为返回 {count, years:[]}(仅 closed 行程、按 t.year 归因年份、去重排序)；reviewBlock() 参数由 visitCount 改为 visit 对象，「去过 N 次 · 2024 2025」年份标签(.poi-years)；世界地图足迹点/历史树行程标签也补 (year)。② 城市库 renderPoints 重写：同城同点按「名称」聚合为**一行卡片**(lib-spot)，多次出现时卡片下方加可折叠的「📋 行程记录」(默认 collapsed，点卡片 toggleSpotLog 展开)，每行=行程标题(年份)·天·时间，可点 jumpToSpot 跳去对应行程编辑(setActiveTrip+setFlow('trip'))；不再按 trip→day 平铺重复行，解决占空间。新增 CSS：.pt-rec-h/.lib-spot/.lib-main/.spot-log.collapsed/.log-row/.log-year/.log-edit + @media(max-width:560px) 收紧间距/字号。recs 推荐块 reviewBlock 传参同步改 {count:0,years:[]}。node --check 通过，已上线 925bc67。
经验：旅游日志类「多次访问同一地点」优先用「一行卡片 + 折叠明细」而非重复行，手机端尤其省空间；年份等时间维度一定要落到数据字段(不要靠当前年推测)，否则跨年展示会错乱。

**本回合（全页 QA 跑通 + 修 2 个真 bug）**：用户要求「跑一下整个页面找 bug + 看建议修改点」。做法：① 静态交叉校验——脚本提取所有 on* 事件处理函数名 vs 已定义函数(250个定义)，无「按钮失效」类悬空引用；② 真机执行——用 jsdom(装到 C:/Users/Administrator/.workbuddy/binaries/node/workspace) 真正加载并执行页面脚本，stub 掉 canvas/getContext、matchMedia、IntersectionObserver、serviceWorker，捕获 console.error/window.error/unhandledrejection，并模拟走完 首页→世界地图→城市→行程点→我的行程 各流程。jsdom 比 browser-use CLI（本环境没装）更可控、能抓到 TDZ 这类运行时错。发现并修复 2 个真 bug：
1. **加载即报 TDZ 错误**：`initTripCode()` 这个 async IIFE 在脚本中段(原 ~2616 行)立即 `render()`，而 `render()` 经 `renderNowBanner→computeNowSpot→getNowDayIdx→getNowDate` 访问 `nowRef`(及 `TRIP_BASE`/`dayView` 等多个 `let/const`)，这些声明都在 IIFE **之后** → `ReferenceError: Cannot access 'X' before initialization`。页面靠末尾(原4193)的 `render()` 能正常显示，但首屏瞬间抛未捕获错误(老用户习以为常、新访客控制台报错)。修复：把该 IIFE 整体挪到脚本最末尾所有变量初始化之后再调用，并给 `autoInitSync` 加 try/catch。
2. **首屏种子城市库为空（新访客真 bug）**：`syncCities()` 只把天的 `city`(slug 如 'bangkok')汇总进 `t.cities`(中文名 曼谷)，但**不回写**天的 city；而 `renderPoints('曼谷')` 用 `d.city === '曼谷'` 过滤 → slug 永远不匹配 → 库显示空。老用户因 localStorage 里天的 city 已被改成中文才不报错。修复：`syncCities()` 第2步把天的 slug 归一化为中文名(逗号多城保留)，并在 `loadAllTrips` 的「无 localStorage 种子分支」也调用 `syncCities(first)`。修复后 jsdom 全 10 项流程通过、0 运行时错误。
**QA 经验**：单文件巨型内联脚本的「初始化顺序」极易出 TDZ——所有 `let/const` 状态变量 + 首屏 `render()` 必须都在同一个末尾 `boot()` 里触发，别在脚本中段用 IIFE 提前 render。建议后续把整段包进 `(function(){... boot();})()` 统一管理初始化顺序。已上线 5a517a5。

**本回合（城市行程点库重构，去掉时间安排）**：用户要求「行程点库=普遍意义的城市行程点总库，不含某一次的时间安排；只列有具体地点地址的；按首字母排序 + 可搜索 + 首字母筛选」。改造 `renderPoints`：
- 数据源改为「去重总库」：遍历所有行程天里的 spot（经 `spotCitiesOf` 归到该城市）+ `POI_DB[city]` 推荐，按名称去重合并；**硬性过滤 `!s.addr`**（无具体地址的地点不进库，如纯「自由活动」）。
- 移除原「按行程→天→时间」分组与「📋 行程记录」展开（`toggleSpotLog`/`jumpToSpot` 已删除），库里不再出现 ⏰ 时间 / 📋多时段。
- 排序：`firstLetter(name)`（A-Z + 中文拼音首字母，用 `PY_OVR` 覆盖当前数据所有中文首字，未收录的归 `#`）+ `Intl.Collator('zh-CN')` 二次排序；首字母索引栏 `pt-index` 动态生成（只显示有结果的字母 + 全部/#），点击 `setPointsLetter` 筛选；搜索框 `pointsSearch` `oninput` 按名称/地址子串过滤。
- 每张卡片：名称 + 📍地址 +（🗂️出现在 N 个行程）+ 备注/注意事项/票务 + 链接 + `reviewBlock`(去过几次+年份+评价)；仅推荐库里的点打 💡推荐 标。
- 写法要点：因 `const POI_DB`/`let TRIPS` 不挂 window，jsdom 提取首字改用「从源码文本抓所有 `name:` 首字」再人工标拼音；`pointsSpots/pointsQuery/pointsLetter` 用顶层 `var` 声明（自动成为 window 属性，供内联 onclick 调用 `setPointsLetter`/`renderPointsList`）。
- 校验：jsdom 全 15 项断言通过（首字母映射 曼→M/大→D/双→S/清→Q/象→D 等；索引栏+分组头存在；首字母筛选 B 后名称全 B 开头；搜索「大」命中含大字；库内无 ⏰/📋；`toggleSpotLog`/`jumpToSpot` 已移除；0 运行时错误）。已上线 b654d67。
- 心得：中文首字母筛选若依赖易错的拼音边界表会出 bug，改用「PY_OVR 覆盖当前数据首字 + 未收录归 #」更稳、且索引栏由实际数据动态生成，天然规避空字母。

**本回合（用户说"全部修改" → 实现之前列的 4 条建议，酒店凭证仍阻塞）**：用户确认把上一回合建议的 5 条修改全做掉（④酒店凭证需重发文件，阻塞）。实现 4 条：
1. **年份真正参与时间状态判定**：`tripTimeStatus(t)` 加年份层面判断——`t.year` 明确时，yr<curY→'closed'(过去行程=已结束)、yr>curY→'upcoming'(未来=待开始)、yr==curY 才走原 M.D 比较；无 year 则回退当前年(向后兼容)。`computeVisitMap`(去过次数)与状态徽标因此跨年/历史行程都准确。新增 `yearColor(y)`(确定性 hsl 色相，(y-2000)*47%360) 与 `cityYearInfo(c)`(rep=最新年份, years=去重排序)。
2. **初始化收敛为 boot()**：脚本末尾把 render/autoInitSync/initLayoutMode/registerSW/initBgDeco/initBear/jumpToNow + hashchange 监听 全部收进 `function boot(){...} boot();` 单一入口，杜绝 TDZ、消除散落的 initTripCode IIFE。
3. **合并卡片支持多版本备注**：`renderPoints` 合并卡片当 `multi` 时不再只显示第一条 notes/warn，主卡改「📝 多条备注（点开卡片查看各次）」提示(.multi-note)；展开的「行程记录」每行带上该次行程自己的 notes(📝)/warn(⚠️)，`.log-row.checked` 加左侧绿条标识实际去了。
4. **世界/首页地图按年份着色+图例**：`renderWorldMap`/`renderHomeMap` 的弧线 stroke 与点 fill/pulse 改用 `yearColor(代表年份)`；多年份城市点加白色虚线环(.wm-multi)；`wmStats`/`homeStats` 追加年份色点图例(.wm-legend/.wm-legend-dot)。

**本回合（城市详情改为「城市指南」：入境须知+交通须知+行程点库）**：用户要求「城市页不需要行程模块以下的详细行程部分，点开每个城市出现当地的入境须知及该城市的交通须知」。经 AskUserQuestion 确认组合方式选「入境+交通+库合并（推荐）」——城市详情=入境须知(国家级通用)+该城市交通须知(按城市)+行程点库(最下方，保留上轮成果)。实现：
- `#pagePoints` 改三段式：顶部「📍 X · 城市指南」+ 返回按钮；入境须知面板(`#cityEntryBody`)+ 交通须知面板(`#cityTransitTitle`/`#cityTransitBody`)+ 行程点库面板(`#pointsBody`)。
- 新增数据 `ENTRY_NOTES`(require[]/must[]) 与 `CITY_TRANSIT`({城市:{sections:[{icon,title,bullets[]}],tip}})，目前覆盖 曼谷(BTS/MRT/AirportRailLink/公交船/Grab)、清迈(Grab/双条车/租摩托/步行)；其他城市走兜底提示。
- 新增 `renderCityEntry()`(渲染国家级入境须知，两栏：入境要求+必带物品 chips) 与 `renderCityTransit(city)`(按城市渲染交通须知，标题=`城市名 交通须知`，无数据则兜底)；在 `renderPoints(city)` 内、`!city` 守卫前调用，确保每次打开城市都刷新。
- 原「我的行程」视图(#tripContentView)里静态的入境/交通块**保留不动**（用户没要求删，避免误删功能）；本次只在城市指南里新增按城市的版本，内容同源不冲突。
- 城市页(#pageCities)描述文案同步改为「城市指南：入境+交通+库」。
- 校验：jsdom 全 6 项断言通过(曼谷 transit 含 BTS、清迈含双条车、entry 两国通用、库在底部、切换城市后 entry 仍在、0 运行时错误)。已上线 8fccf77。
QA：jsdom 11 项断言全过、0 运行时错误、node --check 通过。已上线 1dbef3f。待办：④酒店凭证需用户重发两个文件(Asia Hotel Bangkok 确认号321-10916245 / Thapae Happy House Chiang Mai 确认号1128149412246066)后接入「必带物品→酒店预订单」。

**本回合（城市浏览页移除底部「🗂️ 行程模块」入口框）**：用户跟进上轮"城市指南"的改动，要求"首页也不需要行程模块以下的详细行程"；澄清后确认指的是城市浏览页(#pageCities)最底部那个打开"某次行程详细信息/我的行程(做攻略)"的「🗂️ 行程模块」卡片框(原 765-770 行 + class city-module)。做法：直接删除该框(仅一个 setFlow('trip') 按钮，无 JS 依赖)，并把页面2注释"城市（大洲→国家→城市 + 行程模块）"改为去掉"+ 行程模块"；保留首页底部的「🗓️ 我的行程」按钮与导航栏「我的行程」步骤(入口仍可用)。校验：node 语法(单脚本 258909 字符 ALL GOOD) + jsdom 真机 10 项流程(含断言 .city-module 已不存在、城市库渲染、openCity→城市指南、串联到 trip/home 全通)0 运行时错误。新建 qa_runtime.js(jsdom 全流程模拟)复用此前 stub 方案。双源同步、待推送。
